import 'package:flutter/material.dart';

class StockCommunityWidget extends StatelessWidget {
  const StockCommunityWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
